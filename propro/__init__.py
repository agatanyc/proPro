"""Protein data source wrapper"""

__version__ = '0.1.0'

def find_foods_by_protein_content(protein_content):
    """List all foods having at least the specified `protein_content`. The
    content should be a `float` between 0.0 (meaning "no protein") and 1.0
    (meaning "all calories come from protein"), inclusive.
    """
    pass
